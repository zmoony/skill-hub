#### 生成完整微服务后端项目

```makedown
请基于以下技术栈与需求，一次性生成完整可运行的SpringCloud Alibaba微服务后端项目，包含所有代码、配置、SQL、文档，无任何省略，可直接复制到IDE运行。

### 一、技术栈（严格遵循）
Java8、SpringBoot 2.7.18、SpringCloud Alibaba 2021.0.6.0、Nacos 2.2.3、OpenFeign 3.1.5、MyBatis-Plus 3.5.4.1、PostgreSQL 15.6、Druid、SpringSecurity+JWT、Knife4j 4.5.0、SLF4J+Logback、Lombok 1.18.30、Hutool 5.8.23

### 二、项目结构（严格遵循）
1. 父工程：wva-cloud（统一依赖管理）
2. 公共模块：wva-common（无业务，仅公共支撑）
3. 微服务：
   - wva-gateway（网关）
   - wva-system-management-service（系统管理：字典、主题）
   - wva-user-center-service（用户中心：用户、部门、岗位、角色、菜单、权限）
   - wva-operation-log-service（操作日志：异步采集、查询、导出）
   - wva-business-service（业务预留，仅基础结构）

### 数据库要求
1. 在pg数据库里，database为wva。业务平台的放在自定义的schema，预留。其余的放在wva_admin

### 三、必须完整输出的内容
1. Nacos全局共享配置（wva-global.yml）：统一PG连接、Nacos地址、日志、JWT、跨域、PG LISTEN/NOTIFY等，各微服务bootstrap.yml引用该配置
2. PostgreSQL初始化SQL（wva-admin-init.sql）：
   - 表名以wva-为前缀，主键自增ID，字段加备注，关键字段唯一索引，敏感字段加密
   - 包含表：wva-theme、wva-dict_type、wva-dict_item、wva-user、wva-dept、wva-post、wva-menu、wva-role、wva-user_role、wva-role_menu、wva-operation_log及公共关联表
   - 含初始数据（默认管理员、基础字典、菜单、角色），可直接在PostgreSQL 15.6执行
3. wva-common完整代码：
   - 包：config、entity、utils、security、exception、enums
   - 功能：全局异常、统一返回Result<T>、JSR-380校验、跨域、Logback、分页类、加密工具（AES/BCrypt）、JWT工具、PG LISTEN/NOTIFY消息工具、统一依赖
4. wva-gateway完整代码：路由、全局过滤器、白名单鉴权、统一异常、Nacos、Knife4j
5. wva-system-management-service完整代码：字典/主题CRUD、分页、树形、Feign接口、完整分层
6. wva-user-center-service完整代码：
   - 菜单（树形、按钮权限）、用户（加密、重置密码）、部门/岗位（树形）、角色（编码唯一、批量分配权限）
   - SpringSecurity+JWT：登录、注销、令牌刷新、接口/按钮级权限
   - Feign调用系统管理字典接口
7. wva-operation-log-service完整代码：
   - 基于PG LISTEN/NOTIFY异步采集、监听、消费、重试、持久化
   - 日志字段完整、分页查询、多条件筛选、导出、不可篡改
8. wva-business-service基础结构：包、配置、依赖、公共类，无业务逻辑
9. 环境搭建验证文档：启动顺序、配置检查、Trae IDE关联步骤
10. 接口测试说明：核心接口、参数示例、Knife4j测试流程

### 四、代码规范
- 阿里Java开发手册、RESTful、完整注释
- 统一返回、全局异常、参数校验、事务控制
- 微服务调用、PG消息队列完整实现
- 所有接口支持Knife4j测试
- 命名规范：wva-前缀、驼峰、见名知意

请一次性完整输出所有内容，不要分批次，不要省略，确保可直接运行。
```




```text
请基于以下技术栈，生成一套完整的 SpringCloud Alibaba 微服务后端项目，结构清晰、代码规范、可直接运行。

技术栈：
Java8、SpringBoot 2.7.18
SpringCloud Alibaba 2021.0.6.0
Nacos 2.2.3（服务注册 + 配置中心）
OpenFeign 3.1.5
MyBatis-Plus 3.5.4.1
PostgreSQL 15.6
SpringSecurity + JWT
Knife4j 4.5.0
Druid 连接池
Lombok 1.18.30
Hutool 5.8.23
SLF4J + Logback

项目结构要求：
1. 父工程（统一版本管理、pom.xml、全局依赖）
2. gateway-gateway（网关服务）
3. system-api（系统管理平台服务）
4. business-api（业务服务，预留结构，暂不实现具体逻辑）
5. common-core（公共核心模块：工具类、统一返回、异常、常量、配置）

必须完整实现的模块（必须写全代码）：
【gateway-gateway 网关】
- 路由转发
- 全局过滤器
- 跨域配置
- 接口鉴权（白名单配置）
- 统一异常处理
- 对接 Nacos
- application.yml 完整配置

【system-api 系统管理平台】
完整实现以下功能：
1. 系统用户（增删改查、分页、状态、重置密码）
2. 部门管理（树形结构、增删改查）
3. 菜单/权限管理（树形菜单、按钮权限、路由权限）
4. 角色管理（角色绑定权限、绑定菜单）
5. 登录认证（SpringSecurity + JWT）
6. 操作日志、登录日志（记录、分页查询）
7. 全局统一返回 R<T>
8. 全局异常捕获
9. 参数校验
10. MyBatis-Plus 分页、逻辑删除、自动填充
11. Knife4j 接口文档
12. 完整 PostgreSQL 建表 SQL
13. 标准分层：controller / service / impl / mapper / entity / dto / vo / config / exception / util

【business-api 业务服务】
只生成基础结构、预留包路径、基础配置、基础类，不写业务逻辑，后期扩展。

【common-core 公共模块】
- 统一返回结果 R
- 全局异常
- 基础常量
- 线程池配置
- 日期工具
- JWT 工具
- 雪花算法
- 全局跨域配置
- MyBatis-Plus 配置

代码规范要求：
1. 阿里 Java 开发手册
2. 注释完整
3. 无冗余代码
4. 无空指针风险
5. 接口 RESTful 风格
6. 配置文件完整、可直接运行
7. 所有模块对接 Nacos
8. 支持多环境配置

请一次性生成完整项目结构 + 所有文件代码，不要省略，不要让我补充内容。
```

### 业务侧代码生成

```txt
请基于以下企业级多端统一账号架构标准，设计并实现完整后端逻辑，只输出后端设计、接口、代码、SQL，不涉及任何前端内容。
基于现有管理端的代码服务，不要影响现有的功能

一、整体架构
1. 一套用户体系，支持：1个管理后台 + N个业务端复用
2. 管理端统一配置：哪些用户允许登录哪些业务端
3. 业务端只展示业务数据，无任何用户、权限、菜单配置
4. 管理端与所有业务端**共用同一个登录接口**
5. 统一鉴权、统一用户中心、统一会话机制

二、表结构设计
1. 应用表 wva_application：id、app_code、app_name、status、app_uri
2. 用户-应用关联表 wva_user_application：user_id、app_code
3. 复用现有用户表，不新建用户体系、不新建权限体系

三、登录逻辑（统一接口）
1. 一套用户体系，管理端统一配置用户可访问哪些业务端
2. 所有端共用同一个登录接口 /auth/login
3. 后端通过URL前缀自动识别当前业务端：
   /admin/**    管理端
   /biz1/**     业务端1
   /biz2/**     业务端2
4. 登录时自动根据访问路径获取appCode，并校验用户权限
5. Token中携带appCode，接口拦截器根据appCode做访问隔离
6. 业务端只做业务展示，无任何权限配置功能
7. 管理端负责用户、应用分配、权限控制
8. 支持无限扩展业务端

四、鉴权与接口隔离
1. 接口按前缀隔离：/admin/**、/business/**
2. 统一拦截器：
   - 解析 Token 中的 appCode
   - 管理端 Token 只能访问 /admin/**
   - 业务端 Token 只能访问 /business/**
3. 业务端仅做登录态校验，不做权限配置

五、业务规则
1. 管理端：负责用户管理、应用分配、启用禁用
2. 业务端：仅展示业务数据，无权限配置能力
3. 支持无限扩展新业务端，仅新增 appCode 即可
4. 数据安全：Token 隔离、防越权、防复用

六、输出内容
1. 表结构 SQL（wva_application、wva_user_application）
2. 统一登录接口完整代码
3. JWT 工具类（包含 appCode）
4. 统一鉴权拦截器实现
5. 权限校验核心逻辑
6. 管理端分配用户应用权限的接口设计
7. 整体流程说明
```

一套用户 + 一个登录接口 + 管理端分配可访问应用 + 业务端纯业务展示
```text
- **管理端、业务端共用同一个登录接口**
- **前端传 appCode 区分自己是哪个端**
- **后端统一校验：用户是否允许登录该端**
- **业务端只展示业务数据，无任何权限配置**
- **管理端统一控制哪个用户能进哪个端**
- **一套用户体系，支持 N 个业务端**
```

## 代码审查

```text
请对这套 SpringCloud Alibaba 微服务项目代码进行生产级严格 Code Review，基于 Java8 + SpringBoot2.7 + MyBatis-Plus + PostgreSQL 技术栈，重点审查：

1. 空指针、未判空
2. 异常处理是否规范
3. 事务控制
4. MyBatis-Plus 使用规范
5. 权限控制是否安全
6. 接口是否防越权
7. 代码结构、分层是否合理
8. 配置是否正确
9. Nacos、Feign、Gateway 配置是否正确
10. 潜在 bug、性能风险、安全漏洞

输出：
1. 问题清单
2. 优化建议
3. 修复后的完整代码
4. 总体评分
```

