
### 进展


1. [x] PostgreSQL    → 确保数据库服务启动，已执行初始化SQL
2. [x] Nacos         → 确保注册中心启动，已上传wva-global.yml配置
3. [x] wva-gateway   → 网关服务(端口8080)
4. [x] wva-system-management-service → 系统管理(端口8082)
5. [x] wva-user-center-service       → 用户中心(端口8083)
6. [x] wva-operation-log-service     → 操作日志(端口8084)
7. [ ] wva-business-service          → 业务服务(端口8085,可选)
8. [ ] 单元测试，接口测试

### 环境配置问题

1. 各个微服务的配置混乱，需要手动写需求进行统一整合
2. pom有依赖包丢失问题，需要多次review
3. 200k上下文-glm 
```
第一步（原型→需求）：原型精简后≤80k token，结构化输入，关键需求置顶。
第二步（需求→SQL）：只输入核心需求文档（≤50k），删除原型冗余。
第三步（SQL→代码）：输入需求 + SQL（≤100k），分模块生成（先 hst-common，再微服务）。
全程：单会话、串行，每步生成后保存结果，下一步只输入核心依赖（不重复全量）。
```
4. 微服务的数据库schema分离，需要手动指定
5. 大型项目idea容易卡死
6. glm5.1的模型，初步生成可以，统一修改需要精确投喂，要不然很垃圾


### 模型问题

1. 不要多个不同的插件同时跑，会出现文件混乱